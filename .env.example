# MongoDB
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/pict-german-platform?retryWrites=true&w=majority

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Ably (Real-time features for live quizzes)
# Get your API key from https://ably.com/accounts (free tier available)
# Use the same key for both variables
ABLY_API_KEY=your-ably-api-key
NEXT_PUBLIC_ABLY_CLIENT_KEY=your-ably-api-key

# Optional: Cloudinary
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Application
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Email Configuration (Required for sending classroom invitations)
# For Gmail: Use App Password (https://myaccount.google.com/apppasswords)
# Enable 2-Step Verification first, then generate app password
# For other providers: Check their SMTP settings (Outlook, AWS SES, custom domain, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=QuestEd <your-email@gmail.com>
