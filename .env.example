# MongoDB
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/pict-german-platform?retryWrites=true&w=majority

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Socket.IO (Real-time features - 100% free, unlimited users!)
# For local development, Socket.IO connects to the same server automatically
# For production with separate API server, set this to your API server URL
# NEXT_PUBLIC_SOCKET_URL=http://localhost:3000  # Optional, defaults to current host

# Optional: Cloudinary
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Application
# For local development: http://localhost:3000
# For production: https://quest-ed-phi.vercel.app (or your custom domain)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Microsoft Teams Integration (Optional)
# 1. Register app at https://portal.azure.com/#view/Microsoft_AAD_RegisteredApps/ApplicationsListBlade
# 2. Set Redirect URI: {NEXT_PUBLIC_APP_URL}/api/teams/auth/callback
#    - For local dev: http://localhost:3000/api/teams/auth/callback
#    - For production: https://quest-ed-phi.vercel.app/api/teams/auth/callback
# 3. API Permissions (Delegated): 
#    - User.Read, Team.ReadBasic.All, TeamMember.Read.All
#    - Channel.ReadBasic.All, ChannelMessage.Send, Calendars.ReadWrite
#    - EduAssignments.ReadBasic, EduRoster.ReadBasic, offline_access
# 4. Create client secret under "Certificates & secrets"
MICROSOFT_CLIENT_ID=your-azure-ad-client-id
MICROSOFT_CLIENT_SECRET=your-azure-ad-client-secret

# Email Configuration (Required for sending classroom invitations)
# For Gmail: Use App Password (https://myaccount.google.com/apppasswords)
# Enable 2-Step Verification first, then generate app password
# For other providers: Check their SMTP settings (Outlook, AWS SES, custom domain, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=QuestEd <your-email@gmail.com>
